apiVersion: v1
kind: ConfigMap
metadata:
  name: jesseinit-portfolio-config
  labels:
    app: jesseinit-portfolio
data:
  # Add any environment variables your application needs
  # Example:
  # API_URL: "https://api.example.com"
  # NEXT_PUBLIC_SITE_URL: "https://jesseinit.com"
